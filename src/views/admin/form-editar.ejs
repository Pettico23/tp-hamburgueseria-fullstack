<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto - Admin</title>
    <!-- Este archivo es casi idéntico al de creación, pero tiene dos diferencias clave:

            Rellena los valores (value="<%= producto.nombre %>") con la información actual de la hamburguesa.

            Usa el truco ?_method=PUT en el formulario para indicarle al servidor que es una actualización. -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Iconos FontAwesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Fuente Moderna -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>body { font-family: 'Outfit', sans-serif; }</style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4 font-sans">

    <div class="bg-white rounded-xl shadow-2xl w-full max-w-2xl overflow-hidden border-t-8 border-blue-500">
        
        <!-- Header del Formulario -->
        <div class="bg-gray-50 p-6 border-b border-gray-200 flex justify-between items-center">
            <div>
                <h2 class="text-2xl font-black text-gray-800 flex items-center gap-2">
                    <i class="fas fa-edit text-blue-500"></i> Editar Producto
                </h2>
                <p class="text-sm text-gray-500 mt-1">Estás modificando: <strong class="text-gray-700"><%= producto.nombre %></strong></p>
            </div>
            <a href="/admin/dashboard" class="w-10 h-10 flex items-center justify-center rounded-full bg-gray-200 text-gray-500 hover:bg-gray-300 transition shadow-sm" title="Cancelar">
                <i class="fas fa-times"></i>
            </a>
        </div>

        <!-- 
            FORMULARIO 
            1. Action: Apunta a la ruta /admin/productos/ID
            2. Method: Usamos POST combinado con ?_method=PUT (Truco para que Express entienda que es una actualización)
            3. Enctype: "multipart/form-data" es OBLIGATORIO para poder subir la nueva imagen.
        -->
        <form action="/admin/productos/<%= producto.id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="p-8 space-y-6">
            
            <!-- Campo: Nombre -->
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Nombre del Producto</label>
                <div class="relative">
                    <span class="absolute left-4 top-3.5 text-gray-400"><i class="fas fa-tag"></i></span>
                    <!-- VALUE: Aquí cargamos el nombre actual del producto -->
                    <input type="text" name="nombre" value="<%= producto.nombre %>" 
                           class="w-full pl-10 pr-4 py-3 border-2 border-gray-200 rounded-lg focus:border-blue-500 focus:ring-0 outline-none transition font-medium text-gray-700" required>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Campo: Categoría -->
                <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Categoría</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-gray-400"><i class="fas fa-list"></i></span>
                        <select name="categoria" class="w-full pl-10 pr-10 py-3 border-2 border-gray-200 rounded-lg bg-white focus:border-blue-500 outline-none appearance-none cursor-pointer font-medium text-gray-700">
                            <!-- Usamos un operador ternario para dejar seleccionada la categoría correcta -->
                            <option value="hamburguesas" <%= producto.categoria === 'hamburguesas' ? 'selected' : '' %>>🍔 Hamburguesas</option>
                            <option value="bebidas" <%= producto.categoria === 'bebidas' ? 'selected' : '' %>>🥤 Bebidas</option>
                        </select>
                        <span class="absolute right-4 top-3.5 text-gray-400 pointer-events-none"><i class="fas fa-chevron-down"></i></span>
                    </div>
                </div>

                <!-- Campo: Precio -->
                <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Precio</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-gray-400 font-bold">$</span>
                        <!-- VALUE: Aquí cargamos el precio actual -->
                        <input type="number" name="precio" value="<%= producto.precio %>" min="0" step="0.01"
                               class="w-full pl-10 pr-4 py-3 border-2 border-gray-200 rounded-lg focus:border-blue-500 outline-none transition font-bold text-gray-700" required>
                    </div>
                </div>
            </div>

            <!-- Sección Imagen -->
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Imagen</label>
                
                <div class="flex flex-col md:flex-row gap-6 items-start">
                    
                    <!-- Columna Izquierda: Imagen Actual -->
                    <div class="w-full md:w-1/3 text-center bg-gray-50 p-4 rounded-xl border border-gray-200">
                        <p class="text-xs font-bold text-gray-400 uppercase mb-2">Imagen Actual</p>
                        <img src="<%= producto.imagen %>" class="mx-auto h-32 object-contain" onerror="this.src='https://cdn-icons-png.flaticon.com/512/1160/1160358.png'">
                    </div>

                    <!-- Columna Derecha: Subir Nueva Imagen -->
                    <div class="w-full md:w-2/3 relative border-2 border-dashed border-gray-300 rounded-xl p-6 text-center hover:bg-blue-50 hover:border-blue-400 transition cursor-pointer group">
                        <input type="file" name="imagen" accept="image/*" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10" onchange="previewImage(this)">
                        
                        <div id="upload-content">
                            <i class="fas fa-camera text-3xl text-gray-400 group-hover:text-blue-500 mb-2 transition"></i>
                            <p class="text-gray-600 font-medium text-sm">¿Quieres cambiar la foto?</p>
                            <p class="text-xs text-gray-400 mt-1">Haz clic aquí o arrastra una nueva imagen</p>
                        </div>
                        
                        <!-- Previsualización de la nueva imagen -->
                        <div id="preview-container" class="hidden mt-2">
                            <p class="text-xs font-bold text-green-600 mb-1"><i class="fas fa-check"></i> Nueva imagen seleccionada</p>
                            <img id="img-preview" class="h-24 mx-auto rounded shadow-sm border-2 border-white">
                        </div>
                    </div>
                </div>
                <p class="text-xs text-gray-400 mt-2 ml-1">* Si no subes nada, se mantendrá la imagen anterior.</p>
            </div>

            <!-- Botones -->
            <div class="pt-6 flex gap-4 border-t border-gray-100 mt-4">
                <a href="/admin/dashboard" class="w-1/3 py-3 rounded-lg border-2 border-gray-200 text-gray-500 font-bold text-center hover:bg-gray-100 hover:text-gray-700 transition">
                    Cancelar
                </a>
                <button type="submit" class="w-2/3 py-3 rounded-lg bg-blue-600 text-white font-bold hover:bg-blue-700 shadow-lg transform active:scale-[0.98] transition flex justify-center items-center gap-2">
                    <i class="fas fa-save"></i> Guardar Cambios
                </button>
            </div>

        </form>
    </div>

    <!-- Script para previsualizar la nueva imagen si el usuario elige una -->
    <script>
        function previewImage(input) {
            const uploadContent = document.getElementById('upload-content');
            const previewContainer = document.getElementById('preview-container');
            const previewImage = document.getElementById('img-preview');

            if (input.files && input.files[0]) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    previewImage.src = e.target.result;
                    uploadContent.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                }
                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>

</body>
</html>