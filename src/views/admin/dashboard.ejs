
<!DOCTYPE html>

<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin - Seven Burgers</title>
    <!-- /*donde el administrador podrá:

        Ver la lista de todos los productos con su imagen y precio.

        Acceder al formulario para Crear nuevos productos.

        Botonera para Editar o Eliminar productos existentes.

        (Opcional) Botones para descargar reportes si implementaste esa parte.*/-->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Iconos -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Fuente Moderna -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>body { font-family: 'Outfit', sans-serif; }</style>
</head>
<body class="bg-gray-100 text-gray-800 font-sans">

    <!-- Navbar Superior -->
    <nav class="bg-white shadow-sm border-b border-gray-200 px-8 py-4 flex justify-between items-center">
        <div class="flex items-center gap-4">
            <div class="bg-blue-600 text-white p-2 rounded-lg font-bold text-xl">SB</div>
            <div>
                <h1 class="text-xl font-bold text-gray-800">Panel de Administración</h1>
                <p class="text-xs text-gray-500 font-medium">Bienvenido, <%= user ? user.nombre : 'Admin' %></p>
            </div>
        </div>
        <a href="/logout" class="text-red-500 hover:text-red-700 font-bold text-sm flex items-center gap-2 transition">
            <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
        </a>
    </nav>

    <div class="max-w-7xl mx-auto p-8">

        <!-- Sección de Reportes (Extras del TP) -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-10">
            <!-- Tarjeta PDF -->
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 flex items-center justify-between hover:shadow-md transition">
                <div>
                    <h3 class="text-lg font-bold text-gray-700">Reporte PDF</h3>
                    <p class="text-sm text-gray-400">Descargar listado oficial</p>
                </div>
                <a href="/admin/reportes/pdf" class="bg-red-50 text-red-600 p-3 rounded-lg hover:bg-red-600 hover:text-white transition">
                    <i class="fas fa-file-pdf text-2xl"></i>
                </a>
            </div>

            <!-- Tarjeta Excel -->
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 flex items-center justify-between hover:shadow-md transition">
                <div>
                    <h3 class="text-lg font-bold text-gray-700">Reporte Excel</h3>
                    <p class="text-sm text-gray-400">Exportar datos de ventas</p>
                </div>
                <a href="/admin/reportes/excel" class="bg-green-50 text-green-600 p-3 rounded-lg hover:bg-green-600 hover:text-white transition">
                    <i class="fas fa-file-excel text-2xl"></i>
                </a>
            </div>

            <!-- Tarjeta Productos -->
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 flex items-center justify-between hover:shadow-md transition">
                <div>
                    <h3 class="text-lg font-bold text-gray-700">Total Productos</h3>
                    <p class="text-sm text-gray-400"><%= productos.length %> items activos</p>
                </div>
                <div class="bg-blue-50 text-blue-600 p-3 rounded-lg">
                    <i class="fas fa-box text-2xl"></i>
                </div>
            </div>
        </div>

        <!-- Tabla de Productos -->
        <div class="bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">
            
            <!-- Header de la Tabla -->
            <div class="p-6 border-b border-gray-200 flex justify-between items-center bg-gray-50">
                <h2 class="text-xl font-black text-gray-800">Gestión de Productos</h2>
                <a href="/admin/productos/crear" class="bg-blue-600 text-white px-5 py-2.5 rounded-lg font-bold shadow-md hover:bg-blue-700 hover:shadow-lg transition flex items-center gap-2 transform active:scale-95">
                    <i class="fas fa-plus"></i> Nuevo Producto
                </a>
            </div>

            <!-- Tabla -->
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead class="bg-gray-100 text-gray-600 uppercase text-xs tracking-wider">
                        <tr>
                            <th class="p-4 font-bold border-b">Imagen</th>
                            <th class="p-4 font-bold border-b">Nombre</th>
                            <th class="p-4 font-bold border-b">Categoría</th>
                            <th class="p-4 font-bold border-b">Precio</th>
                            <th class="p-4 font-bold border-b text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% if (productos.length > 0) { %>
                            <% productos.forEach(prod => { %>
                                <tr class="hover:bg-blue-50 transition duration-150 group">
                                    <td class="p-4 w-24">
                                        <div class="h-16 w-16 rounded-lg bg-white border border-gray-200 p-1 flex items-center justify-center overflow-hidden">
                                            <img src="<%= prod.imagen %>" alt="img" class="h-full w-full object-contain" onerror="this.src='https://cdn-icons-png.flaticon.com/512/1160/1160358.png'">
                                        </div>
                                    </td>
                                    <td class="p-4 font-bold text-gray-800 text-lg">
                                        <%= prod.nombre %>
                                    </td>
                                    <td class="p-4">
                                        <span class="px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wide <%= prod.categoria == 'hamburguesas' ? 'bg-orange-100 text-orange-700' : 'bg-cyan-100 text-cyan-700' %>">
                                            <%= prod.categoria %>
                                        </span>
                                    </td>
                                    <td class="p-4 font-bold text-green-600">
                                        $<%= prod.precio %>
                                    </td>
                                    <td class="p-4 text-center">
                                        <div class="flex items-center justify-center gap-2 opacity-100 md:opacity-60 md:group-hover:opacity-100 transition">
                                            <!-- Editar -->
                                            <a href="/admin/productos/<%= prod.id %>/editar" class="w-9 h-9 rounded-lg bg-white border border-gray-200 text-blue-500 hover:bg-blue-500 hover:text-white hover:border-blue-500 flex items-center justify-center transition shadow-sm" title="Editar">
                                                <i class="fas fa-pen"></i>
                                            </a>
                                            <!-- Eliminar -->
                                            <form action="/admin/productos/<%= prod.id %>?_method=DELETE" method="POST" onsubmit="return confirm('¿Seguro que quieres eliminar este producto?');">
                                                <button type="submit" class="w-9 h-9 rounded-lg bg-white border border-gray-200 text-red-500 hover:bg-red-500 hover:text-white hover:border-red-500 flex items-center justify-center transition shadow-sm" title="Eliminar">
                                                    <i class="fas fa-trash-alt"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" class="p-10 text-center text-gray-400">
                                    <div class="flex flex-col items-center">
                                        <i class="fas fa-hamburger text-6xl mb-4 opacity-20"></i>
                                        <p class="text-lg">No hay productos registrados.</p>
                                        <a href="/admin/productos/crear" class="mt-2 text-blue-600 hover:underline font-bold">¡Agrega el primero!</a>
                                    </div>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            
            <!-- Footer Tabla -->
            <div class="p-4 bg-gray-50 border-t border-gray-200 text-xs text-center text-gray-400">
                Sistema de Gestión v1.0 - Seven Burgers
            </div>
        </div>

    </div>

</body>
</html>