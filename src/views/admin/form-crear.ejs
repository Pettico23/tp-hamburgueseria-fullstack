<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuevo Producto - Admin</title>
    <!-- Este archivo es el formulario para Crear nuevos productos. He mantenido el estilo visual consistente con el Dashboard y el formulario de Edición, utilizando Tailwind CSS y la fuente Outfit. -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Iconos FontAwesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Fuente Moderna -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>body { font-family: 'Outfit', sans-serif; }</style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4 font-sans">

    <div class="bg-white rounded-xl shadow-2xl w-full max-w-2xl overflow-hidden border-t-8 border-green-500">
        
        <!-- Header del Formulario -->
        <div class="bg-gray-50 p-6 border-b border-gray-200 flex justify-between items-center">
            <div>
                <h2 class="text-2xl font-black text-gray-800 flex items-center gap-2">
                    <i class="fas fa-plus-circle text-green-500"></i> Nuevo Producto
                </h2>
                <p class="text-sm text-gray-500 mt-1">Ingresa los detalles de la nueva hamburguesa o bebida.</p>
            </div>
            <a href="/admin/dashboard" class="w-10 h-10 flex items-center justify-center rounded-full bg-gray-200 text-gray-500 hover:bg-red-500 hover:text-white transition shadow-sm" title="Cerrar">
                <i class="fas fa-times"></i>
            </a>
        </div>

        <!-- Formulario Principal -->
        <!-- NOTA: 'enctype="multipart/form-data"' es CRUCIAL para poder subir archivos (imágenes) -->
        <form action="/admin/productos" method="POST" enctype="multipart/form-data" class="p-8 space-y-6">
            
            <!-- Campo: Nombre -->
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Nombre del Producto</label>
                <div class="relative">
                    <span class="absolute left-4 top-3.5 text-gray-400"><i class="fas fa-hamburger"></i></span>
                    <input type="text" name="nombre" placeholder="Ej: Triple Bacon Cheddar" 
                           class="w-full pl-10 pr-4 py-3 border-2 border-gray-200 rounded-lg focus:border-green-500 focus:ring-0 outline-none transition font-medium text-gray-700 placeholder-gray-300" required>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Campo: Categoría -->
                <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Categoría</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-gray-400"><i class="fas fa-list-ul"></i></span>
                        <select name="categoria" class="w-full pl-10 pr-10 py-3 border-2 border-gray-200 rounded-lg bg-white focus:border-green-500 outline-none appearance-none cursor-pointer font-medium text-gray-700">
                            <option value="hamburguesas">🍔 Hamburguesas</option>
                            <option value="bebidas">🥤 Bebidas</option>
                        </select>
                        <span class="absolute right-4 top-3.5 text-gray-400 pointer-events-none"><i class="fas fa-chevron-down"></i></span>
                    </div>
                </div>

                <!-- Campo: Precio -->
                <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Precio</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-gray-400 font-bold">$</span>
                        <input type="number" name="precio" placeholder="0.00" min="0" step="0.01"
                               class="w-full pl-10 pr-4 py-3 border-2 border-gray-200 rounded-lg focus:border-green-500 outline-none transition font-bold text-gray-700 placeholder-gray-300" required>
                    </div>
                </div>
            </div>

            <!-- Campo: Imagen (Subida con Drag & Drop visual) -->
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2 uppercase tracking-wide">Imagen del Producto</label>
                
                <div class="relative border-2 border-dashed border-gray-300 rounded-xl p-8 text-center hover:bg-green-50 hover:border-green-400 transition cursor-pointer group" id="drop-zone">
                    <!-- Input invisible que cubre todo el área -->
                    <input type="file" name="imagen" accept="image/*" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10" onchange="previewImage(this)">
                    
                    <!-- Contenido visual por defecto -->
                    <div id="upload-content" class="space-y-2">
                        <div class="w-16 h-16 bg-gray-100 text-gray-400 rounded-full mx-auto flex items-center justify-center group-hover:bg-green-100 group-hover:text-green-500 transition">
                            <i class="fas fa-cloud-upload-alt text-3xl"></i>
                        </div>
                        <p class="text-gray-500 font-medium group-hover:text-gray-700">Arrastra una imagen o haz clic aquí</p>
                        <p class="text-xs text-gray-400">Soporta JPG, PNG, WEBP</p>
                    </div>

                    <!-- Contenedor para la previsualización -->
                    <div id="preview-container" class="hidden">
                        <img id="img-preview" class="max-h-48 mx-auto rounded-lg shadow-lg border-4 border-white">
                        <p class="text-xs text-green-600 font-bold mt-2"><i class="fas fa-check"></i> Imagen seleccionada</p>
                        <p class="text-xs text-gray-400">(Haz clic de nuevo para cambiarla)</p>
                    </div>
                </div>
            </div>

            <!-- Botones de Acción -->
            <div class="pt-6 flex gap-4 border-t border-gray-100 mt-4">
                <a href="/admin/dashboard" class="w-1/3 py-3 rounded-lg border-2 border-gray-200 text-gray-500 font-bold text-center hover:bg-gray-100 hover:text-gray-700 transition">
                    Cancelar
                </a>
                <button type="submit" class="w-2/3 py-3 rounded-lg bg-green-600 text-white font-bold hover:bg-green-700 shadow-lg transform active:scale-[0.98] transition flex justify-center items-center gap-2">
                    <i class="fas fa-save"></i> Guardar Producto
                </button>
            </div>

        </form>
    </div>

    <!-- Script para previsualizar la imagen antes de subirla -->
    <script>
        function previewImage(input) {
            const uploadContent = document.getElementById('upload-content');
            const previewContainer = document.getElementById('preview-container');
            const previewImage = document.getElementById('img-preview');
            const dropZone = document.getElementById('drop-zone');

            if (input.files && input.files[0]) {
                const reader = new FileReader();

                reader.onload = function(e) {
                    previewImage.src = e.target.result;
                    
                    // Ocultar placeholder y mostrar imagen
                    uploadContent.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                    
                    // Cambiar borde para indicar éxito
                    dropZone.classList.add('border-green-500', 'bg-green-50');
                    dropZone.classList.remove('border-gray-300');
                }

                reader.readAsDataURL(input.files[0]);
            } else {
                // Resetear si no hay archivo
                uploadContent.classList.remove('hidden');
                previewContainer.classList.add('hidden');
                dropZone.classList.remove('border-green-500', 'bg-green-50');
                dropZone.classList.add('border-gray-300');
            }
        }
    </script>

</body>
</html>